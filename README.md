# OC-tools About
This is a repository containing my programs for OpenComputers installed By OPPM.
# Setup
Once you install the oppm via floppy then you can run
``` oppm register theluafox86/oc-tools ```bash
